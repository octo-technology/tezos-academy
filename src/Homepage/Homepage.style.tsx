import styled from "styled-components/macro";
import { AnimatedCard } from "../styles";

export const HomepageStyled = styled(AnimatedCard)`
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  grid-gap: 20px;
`;
